import App from './components/App';

const widgets = [{
  title: 'Angler.js',
  Component: App,
  useCase: [{
    title: 'Stage renderer',
    description: 'React-based renderer for stage',
    paths: ['/',
'index.html',
'stories.html'],
  }],
}];

export default widgets;
